# Starter Project

## Django

Run `poetry install` and then the following commands to start the backend:

```bash
poetry run python manage.py migrate
poetry run python manage.py runserver
```

## Nuxt.js

Run `yarn` and then `yarn dev` to start the dev server

## docker compose

Run `docker compose up` to start backend and frontend with postgresql and redis